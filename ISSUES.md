# 本地修复问题清单

本文件记录本次本地修复范围。所有修改在审核前只保留在本地分支 `fix/security-data-integrity`，不会推送到 GitHub。

| 状态 | 优先级 | 问题 | 影响 |
| --- | --- | --- | --- |
| 已修复 | P0 | `Schema.sql` 未创建 `common_exercises` | 新数据库初始化后公用动作接口失败 |
| 已修复 | P0 | 同步接口按 UID 更新时缺少用户归属条件 | 可能跨用户篡改训练记录或自定义动作 |
| 已修复 | P0 | 动作名称直接拼入 `innerHTML`/内联事件 | 恶意动作名称可触发存储型 XSS |
| 已修复 | P1 | 首次登录自动认领全部匿名数据 | 首个用户可能看到其他来源的历史数据 |
| 已修复 | P1 | 任意登录用户可以修改公共动作 | 全局共享数据可被普通用户篡改 |
| 已修复 | P1 | Hono 依赖存在高危审计问题 | Worker 使用存在已知安全漏洞的依赖 |
| 已修复 | P2 | README、Schema 文件名和实际目录不一致 | 新成员按文档部署容易失败 |
| 已修复 | P2 | 缺少自动化回归测试和部署前检查 | 安全修复容易被后续改动破坏 |
| 已修复 | P0 | `package-lock.json` 含未解决的 Git 冲突标记 | 依赖安装和审计结果不可复现 |
| 已修复 | P1 | 前端页面缺少清晰层级、液态玻璃材质和系统动效 | 首页与训练页面缺少 iOS 风格的空间层次和反馈 |
| 已修复 | P1 | 序号与图标重叠、圆角不统一、输入和主操作配色突兀 | 首页信息层级不清，训练页控件与玻璃材质脱节 |
| 已修复 | P1 | 玻璃效果过于扁平、动效反馈不足 | 背景透出、高斯模糊、入场和按压反馈不明显 |

## 约定

- 公共动作写权限通过 `COMMON_EXERCISE_ADMIN_IDS` 配置，值为逗号分隔的 GitHub 用户名或 GitHub 数字 ID；未配置时默认只读。
- 历史匿名记录保留在数据库中，但不再自动归属或展示给普通用户；如需迁移，须由维护者执行经过审核的 SQL 迁移。
