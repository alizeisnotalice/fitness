# Wrangler configuration file
name = "fitness-tracker"
main = "src/index.js"
compatibility_date = "2024-03-01"

# 公用动作写权限：填写逗号分隔的 GitHub 用户名或数字 ID；未配置时只读。
# 生产环境建议使用 `wrangler secret put COMMON_EXERCISE_ADMIN_IDS`。
# [vars]
# COMMON_EXERCISE_ADMIN_IDS = "github-login-or-id"

# 将 D1 数据库绑定到 Worker
[[d1_databases]]
binding = "DB" # 在 Worker 中使用的绑定名称
database_name = "fitness-data" # 你给数据库起的名字
database_id = "f6468305-b3d1-4914-baef-46120bef85c4"
